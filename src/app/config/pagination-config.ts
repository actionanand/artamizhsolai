/**
 * Pagination and display configuration for different pages
 */
export const paginationConfig = {
  // Blog page: items per page
  blogPageSize: 10,
  
  // Archive page: items per page
  archivePageSize: 25,
  
  // Recent posts in article detail page: max items to display
  articleRecentPostsCount: 5,
  
  // Recent posts in home page: max items to display
  homeRecentPostsCount: 3
};
